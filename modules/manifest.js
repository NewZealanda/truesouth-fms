// ─────────────────────────────────────────────────────────────────────────────
// RETIRED MODULE — legacy Manifest + Seatmap (v23.76).
//
// The legacy manifest/seatmap UI was replaced by the Rezdy-powered Operations flow
// (Bookings / Seatmap / Loadsheets, see rezdy.js). This module is no longer part of
// the build (removed from ORDER in build.py). The one helper that was still live —
// cogPillClass() — now lives in shared.js.
//
// Kept as an empty tombstone only because the sandbox can't delete the file; it is
// NOT concatenated into index.html. Safe for Andrew to `git rm` locally.
// ─────────────────────────────────────────────────────────────────────────────
